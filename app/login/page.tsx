import { RoleLogin } from '@/components/role-login'

export const metadata = {
  title: 'Sign in | STUBOT',
  description: 'Sign in to your STUBOT student or staff workspace.',
}

export default function LoginPage() {
  return <RoleLogin />
}
